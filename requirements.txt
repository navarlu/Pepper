# Direct dependencies of the kept code only — transitive packages are
# resolved by pip/uv. `qi` (NAOqi) is not a pip package: it is
# bind-mounted into the bridge / safe-startup containers.

# LiveKit (media + agents)
livekit==1.0.23
livekit-agents==1.3.12
livekit-api==1.0.7
livekit-protocol==1.1.0
livekit-plugins-openai==1.3.12
livekit-plugins-silero==1.3.12
onnxruntime==1.23.1  # silero VAD runtime

# OpenAI
openai==2.7.1

# HTTP / scraping (tools, tablet-server)
aiohttp==3.13.2
requests==2.32.5
beautifulsoup4==4.13.4

# Audio / imaging (user-client, tablet QR)
numpy==2.3.4
sounddevice==0.5.3
pillow==12.0.0
segno==1.6.1

python-dotenv==1.2.1
