# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
env/
venv/
ENV/
__pypackages__/
.coverage
htmlcov/
.ipynb_checkpoints

# Editors / OS
.DS_Store
.idea/
.vscode/

# Docker volumes from the old thesis stack (root-owned, unreadable by git).
docker/redis-data/
docker/weaviate_data/

# Local config + secrets
.env
docker/.env
docker/livekit/turn.key
docker/livekit/turn.crt
.claude
.mcp.json
AGENTS.md
CLAUDE.md

# Runtime data (tokens, state.json, recordings, annotation metadata).
# The animation catalog is the one data file the agent needs in-repo.
data/
!voice-agent/src/data/
voice-agent/src/data/*
!voice-agent/src/data/animation_catalog.json
# robot/data: animations.json + the annotation metadata the catalog is
# built from. The source video recordings (~3.9 GB) stay local.
!robot/data/
robot/data/animation_metadata/*
!robot/data/animation_metadata/metadata/

# Logs + local scratch
*.log
logs/
*.wav
notes.txt
run*.txt
backups/**

# Old thesis output left on disk (results ~4.3 GB, test outputs).
voice-agent/src/experiment/
